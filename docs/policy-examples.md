# Policy Examples

Policies in this repository are mock examples. They do not enforce production controls.

## Example Rules

- Require approval for stablecoin transfers above a configured amount.
- Require review when a sandbox address-risk adapter returns a review signal.
- Restrict transfers from frozen wallets.
- Emit audit events for administrative changes.

Production policy enforcement remains private by design.
